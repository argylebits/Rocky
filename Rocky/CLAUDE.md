# Rocky — Claude Code Instructions

## Status: STUB ONLY

Rocky is a placeholder for a future native macOS/Linux/Windows app built with SwiftCrossUI.

## Rules

- **Do not implement anything here** — this package is a stub
- **Do not add SwiftCrossUI as a dependency yet** — wait until native app development begins
- **Do not modify `Rocky.swift`** — leave it as a minimal stub that compiles
- **All work goes in RockyCore and RockyCLI** — ignore this package entirely for now

## Future plan

When native app development begins:
- Add SwiftCrossUI dependency
- Import RockyCore for all business logic
- Build UI on top of the same RockyCore services used by RockyCLI

## Current stub

`Rocky.swift` should contain only enough to compile:

```swift
import Foundation
// Rocky — native app coming soon
```
