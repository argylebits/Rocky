{
  "version": 1,
  "settings": {
    "auto-stop": {
      "type": "bool",
      "default": true,
      "description": "Automatically error when starting a timer on a project that already has one running"
    }
  }
}
